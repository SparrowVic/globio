import {
  HologramStarfieldLayer,
  type HologramStarfieldLayerOptions,
} from '../hologram/starfield';

export class CinematicStarfieldLayer extends HologramStarfieldLayer {
  public constructor(options: HologramStarfieldLayerOptions) {
    super(options);
  }
}
